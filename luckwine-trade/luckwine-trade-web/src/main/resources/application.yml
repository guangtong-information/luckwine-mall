# 配置文件加密因子key
jasypt:
  encryptor:
    password: luckwine_trade



#数据源配置 MyBatisConfig.class
spring:
  application:
    name: luckwine-trade
  profiles:
    active: dev
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/luckwine-trade?allowMultiQueries=true&useUnicode=true&characterEncoding=utf-8&zeroDateTimeBehavior=convertToNull&useSSL=false
    username: root
    password: Aa123456
    # 使用druid数据源
    type: com.alibaba.druid.pool.DruidDataSource

mybatis:
  mapper-locations: classpath:/mapper/*.xml
  config-location: classpath:/mybatis-config.xml

# dubbo最新配置，整合了注册中心配置
dubbo:
  protocol:
    port: -1
    name: dubbo
  registry:
    address: nacos://192.168.1.250:8848
    #    parameters:
    #      namespace: 5f5afe75-b943-40dc-ae5a-6f3e9ab24cf8
    timeout: 6000
    version: 1.0.0
    check: true
    file: ./.dubbo/luckwine_trade_registry
  scan:
    base-packages: com.luckwine.trade
  provider:
    version: 1.0.0
    timeout: 6000
    filter: dubboLogFilter
    validation: true
    group: dev
  consumer:
    check: false
    version: 1.0.0
    group: dev

#日志相关
logging:
  config: classpath:logback.xml

#springboot管理相关，结合pringboot actuator一起使用
management:
  info:
    git:
      mode: full
    build:
      enabled: true
    env:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
  health:
    db:
      enabled: true
    #    redis:
    #      enabled: true
    diskspace:
      enabled: true
  endpoint:
    shutdown:
      enabled: true
    health:
      enabled: true
      show-details: always
    info:
      enabled: true

#酒缘网自营店铺
store: 88888888

#流水号：环境标志位
env: 0

#酒缘网收款大账号
recAcct: 1001811040141026993481002